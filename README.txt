Is a simple checker that looks a string for the passed words, and returns a boolean.

----------------------------------------------------------------------------------------------

Um simples checador que procura numa string as palavras passadas, e retorna um boolean.